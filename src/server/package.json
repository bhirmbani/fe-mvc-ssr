{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "cross-env-shell babel index.tsx -d dist --source-maps --extensions '.tsx'",
    "build-server": "npm run clean && npm run compile",
    "start-server": "npm run build-server && node dist/index.js",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.10",
    "@babel/core": "^7.13.10",
    "@babel/preset-react": "^7.12.13",
    "@types/express": "^4.17.11",
    "babel-preset-env": "^1.7.0",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "webpack": "^5.25.0",
    "webpack-cli": "^4.5.0"
  },
  "type": "module"
}
